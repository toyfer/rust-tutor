fn main() {
    let arr: [i32; 5] = [1, 2, 3, 4, 5]; // 5つのi32型の要素を持つ配列を宣言
    println!("配列の要素数: {}", arr.len()); // 出力: 配列の要素数: 5
    println!("最初の要素: {}", arr[0]); // 出力: 最初の要素: 1
    // arr[5] = 6; // エラー：配列の範囲外にアクセスしようとしています
}
