fn main() {
    let arr = [1, 2, 3, 4, 5];
    let slice = &arr[1..4]; // arrの2番目から4番目の要素までのスライスを作成
    println!("スライスの要素数: {}", slice.len()); // 出力: スライスの要素数: 3
    println!("スライスの最初の要素: {}", slice[0]); // 出力: スライスの最初の要素: 2
}
